# Embedding runs locally on CPU: no API key, no network after the first run,
# and identical vectors on every run - which is what keeps results from
# different days comparable.
#
# sentence-transformers pulls in PyTorch, so expect roughly 1-2 GB of
# dependencies plus the model weights on first use. The exact size depends on
# your platform and whether you get a CPU or GPU build of torch.
# Everything before the embedding step (corpus loading, chunking, the eval
# validator) is standard library only and needs none of it.

sentence-transformers>=3.0
numpy>=1.24

# Generation only. Needs ANTHROPIC_API_KEY, either exported in the shell or
# placed in a .env file (see .env.example). Every other stage runs without it.
anthropic>=0.40
python-dotenv>=1.0

# Only needed for the OpenAI embedding models; the local ones run without it.
openai>=1.40
